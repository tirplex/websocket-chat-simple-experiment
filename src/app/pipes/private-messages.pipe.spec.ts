import { PrivateMessagesPipe } from './private-messages.pipe';

describe('PrivateMessagesPipe', () => {
  it('create an instance', () => {
    const pipe = new PrivateMessagesPipe();
    expect(pipe).toBeTruthy();
  });
});
